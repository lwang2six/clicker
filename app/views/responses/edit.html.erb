<h1>Editing response</h1>

<%= render 'form' %>

    <%= link_to 'Show', problem_set_question_response_path(@problem_set.id, @question.id, response.id) %> |
    <%= link_to 'Back', problem_set_question_responses_path %>
