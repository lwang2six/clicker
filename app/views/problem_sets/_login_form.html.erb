<%= form_for(@problem_set) do |f| %>
  <% if @problem_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@problem_set.errors.count, "error") %> prohibited this problem_set from being saved:</h2>

      <ul>
      <% @problem_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <h2>Please enter User ID before you begin</h2>
  <div class="fields">
    <label>User ID #:</label>
    <input id="user_id" name="user_id" size="30" type="text" value="">
  </div>
  <br/>
  <div class="actions">
    <%= f.submit "ENTER"%>
  </div>
<% end %>
